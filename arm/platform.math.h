#pragma once
#include <stdio.h>

namespace vision {
  inline namespace platform {
    inline void add_() {
        // wonderfull code
        printf("arm add\n");
        // wonderfull code
    }

    void mul_();  
  
    // math.h
    template<typename T> inline void addT_() {
      // wonderfull code
      printf("arm addT\n");
      // mode code
      // even more code
    }
  }
}
